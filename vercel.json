{
  "version": 2,
  "buildCommand":"nuxt build",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/main.ts",
      "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "HEAD", "OPTIONS"]
    }
  ]
}
